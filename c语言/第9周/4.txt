#include<stdio.h>
#include<stdlib.h>
#define N 
void ZigzagMatrix(int a[][N], int n);
int main()
{
	int a[N][N],n,i,k,count=0;
	printf("Input n:\n");
	scanf("%d",&n);
	if(n>0&&n<=100&&n%2!=0)
	{
		for(i=0;i<n;i++)
		{
			a[i][0]=i+1;
			cont++;
		}
	}
	else if(n>0&&n<=100&&n%2==0)
	{
		for(k=n-1,p=n-1;k>=0&&p>=0;p--)
		{
			a[k][p]=n*n;
			for()
			
	else
	{
		printf("Input error!\n");
	}